import React from "react";

function StaticPage() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>Welcome to Static Page</h1>
    </div>
  );
}

export default StaticPage;
